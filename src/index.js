import { loadRandomPokemon, loadPokedex, listPokemons } from './ui.js';

function update() {

}

async function start() {
  loadRandomPokemon();
  // loadPokedex(update);
  // listPokemons(update);
}

start();
